{% if mixture.debug %}<!-- header.liquid -->
{% endif %}<!doctype html>
<html lang="en">
<head>
	<!-- DON'T JUDGE MY HTML! IT'S GENERATED DYNAMICALLY AND I DID MY BEST! -->
  <meta charset="utf-8">
  {% include "global/title" %}
{% if model.debug %}
  {{ "app.min.css" | asset_url | stylesheet_tag }}
{% else %}
  {{ "app.css" | asset_url | stylesheet_tag }}
{% endif %}
</head>