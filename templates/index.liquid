{% block content %}

  {% if mixture.debug %}<!-- index.liquid -->{% endif %}

  {% collection "portfolio" %}
    <ul class="grid">
    {% for post in collection.items %}<!--
	 -->{% include "portfolio/thumbnail" with post %}<!--
 -->{% endfor %}
		</ul>
  {% endcollection %}

{% endblock %}